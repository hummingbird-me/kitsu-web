{{#bootstrap/bs-modal id=modalId onClose=onClose as |modal|}}
  {{#modal.body}}
    {{media.yearlessTitle}}
    {{media.year}}
    {{remaining}}
    {{one-way-textarea content
      update=(action (mut content))
      placeholder="React to dis shit"
    }}
    <button onclick={{perform createReaction}}>Post</button>
  {{/modal.body}}
{{/bootstrap/bs-modal}}
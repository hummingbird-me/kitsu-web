<h6>{{link.site.name}}</h6>
<div class="form-group row">
  <div class="col-xs-12">
    <label class="col-form-label">
      Enter your {{link.site.name}} details to add a link to your profile.
    </label>
  </div>
  <div class="col-xs-12">
    {{one-way-text link.url
                    update=(action "update")
                    class="form-control"
                    placeholder=placeholder}}
    {{#if (or (not link.isNew) (and (eq link.site.name "Website") link.url))}}
      <div class="profile-link-remove">
        <a href="#" onclick={{action "remove"}}>Remove from profile</a>
      </div>
    {{/if}}
  </div>
</div>
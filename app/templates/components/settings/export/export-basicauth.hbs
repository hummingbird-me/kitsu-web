<div class="modal-wrapper">
  <div class="auth-section active">
    <h6 class="auth-section-title">Sync with {{siteName}}</h6>
    <p class="auth-section-helper">We'll update your anime and manga lists on {{siteName}} every time you update them on Kitsu. Note that we won't delete entries from your {{siteName}} library.</p>
    <form>
      <div class="form-group">
        {{one-way-text username
          update=(action (mut username))
          class="form-control"
          placeholder="Username"
          autofocus="autofocus"
          data-test-selector="username"}}

        {{one-way-password password
          update=(action (mut password))
          class="form-control"
          placeholder="Password"
          data-test-selector="password"}}
      </div>
      <div class="form-cta">
        <button type="submit" class="button button--primary" disabled={{if (or (is-empty username) (is-empty password)) "disabled"}} {{action "setupExport"}}>
          {{#if createExport.isRunning}}
            {{loading-spinner size="small"}}
          {{else}}
            Sync to {{siteName}}
          {{/if}}
        </button>
      </div>
    </form>
  </div>
</div>

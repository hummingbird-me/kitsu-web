{{#each ratings as |rating|}}
  <div class="simple-rating-wrapper hint--rounded hint--bounce hint--top" aria-label={{t (concat "library-entry.rating." rating.tag)}} {{action "onClick" rating}}>
    {{svg-jar rating.tag
      width="50px"
      height="50px"
    }}
  </div>
{{/each}}
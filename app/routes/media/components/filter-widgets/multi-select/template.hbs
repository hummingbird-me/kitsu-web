{{#if includeAll}}
  <li class={{if (is-empty selected) "active"}} {{action "clear"}}>
    {{yield "All"}}
  </li>
{{/if}}
{{#each selection as |option|}}
  <li class={{if (contains option selected) "active"}} {{action "toggle" option}}>
    {{yield option}}
  </li>
{{/each}}
{{#bootstrap/bs-modal id="auth-modal" onClose=(action "onClose") as |modal|}}
  {{#modal.body}}
    {{component componentName
      changeComponent=(action "changeComponent")
      close=(action "closeModal")}}
  {{/modal.body}}
{{/bootstrap/bs-modal}}